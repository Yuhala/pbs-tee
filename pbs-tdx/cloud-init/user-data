#cloud-config
hostname: builder-vm
users:
  - name: ubuntu
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: sudo
    shell: /bin/bash
    plain_text_passwd: '123456'
    lock_passwd: false
ssh_pwauth: true
chpasswd:
  list: |
    ubuntu:123456
  expire: false
disable_root: false
